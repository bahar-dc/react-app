import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root2 = ReactDOM.createRoot(document.getElementById('page-content'));
root2.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);